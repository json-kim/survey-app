import 'package:flutter/material.dart';

class SliderSurvey extends StatelessWidget {
  const SliderSurvey({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
